import { List } from 'immutable';
import { ISectorField } from './sectorfields.types';

// export const INITIAL_STATE = List<ICourseField>([new CourseFieldRecord({})]);
export const INITIAL_STATE = List<ISectorField>();
